# pip install -r requirements.txt

# base ----------------------------------------
# pip install -r requirements.txt

# Core / training
Cython
numpy>=1.21.0
opencv-python>=4.1.2
Pillow>=7.0
PyYAML>=5.3
scipy>=1.4.1
tensorboard>=2.2
torch>=1.7.0
torchvision>=0.8.1
tqdm>=4.41.0

# Logging / experiment tracking (optional)
wandb

# Plotting / analysis
matplotlib>=3.2.2
seaborn
pandas

# Dataset / metrics
pycocotools>=2.0

# Profiling / extras
thop  # FLOPS computation

# Optional export / inference (commented — enable if needed)
# onnx>=1.8.0
# coremltools==4.0
# scikit-learn>=0.24

# Development helpers
pytest

# Notes:
# - Use a matching PyTorch build for your platform (CPU, CUDA, MPS). Example:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# - If you run on servers without a display, prefer `opencv-python-headless` instead of `opencv-python`.
