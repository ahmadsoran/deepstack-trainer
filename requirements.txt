# DeepStack Trainer Requirements
# Updated for security and compatibility (January 2024)

# Core dependencies with pinned versions for security
torch>=2.0.0,<2.2.0  # Latest stable with security patches
torchvision>=0.15.0,<0.17.0  # Compatible with torch version
numpy>=1.24.0,<1.26.0  # Security patches for array operations
opencv-python>=4.8.0,<4.10.0  # Latest with security fixes
Pillow>=10.0.0,<11.0.0  # Security updates for image processing
PyYAML>=6.0.0,<7.0.0  # Security fixes for YAML parsing
scipy>=1.10.0,<1.12.0  # Scientific computing with security patches
tensorboard>=2.13.0,<2.16.0  # Latest with security updates
tqdm>=4.65.0,<4.67.0  # Progress bars with security fixes

# Logging and experiment tracking
wandb>=0.15.0,<0.17.0  # Latest with security patches

# Plotting and analysis
matplotlib>=3.7.0,<3.9.0  # Security updates for plotting
seaborn>=0.12.0,<0.14.0  # Statistical visualization
pandas>=2.0.0,<2.2.0  # Data manipulation with security fixes

# Dataset and metrics
pycocotools>=2.0.6,<2.1.0  # COCO dataset tools

# Profiling and performance
thop>=0.1.1,<0.2.0  # FLOPS computation

# Development and testing
pytest>=7.4.0,<7.5.0  # Testing framework
pytest-cov>=4.1.0,<4.2.0  # Coverage reporting
black>=23.0.0,<24.0.0  # Code formatting
flake8>=6.0.0,<7.0.0  # Linting
mypy>=1.5.0,<1.8.0  # Type checking
bandit>=1.7.0,<1.8.0  # Security linting

# Optional dependencies (uncomment if needed)
# onnx>=1.14.0,<1.16.0  # Model export
# coremltools>=7.0,<8.0  # CoreML export
# scikit-learn>=1.3.0,<1.4.0  # Machine learning utilities

# Security notes:
# - All versions pinned to prevent supply chain attacks
# - Regular security updates recommended
# - Use `pip-audit` to check for vulnerabilities
# - Consider using `pip-tools` for dependency management
